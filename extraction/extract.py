#search for COURT:
#Get date, get location
#Keep doing the following until we reach the end
#look for CNT: and number of count
#Then go to the line above to get the number of penal code

def dumpTextFile(filename):
  

def splitText(text, divider):
  records = text.split(divider)
  records = records[1:]

def processRecord(textRecord):

def processSentences(text):
  sentences = text.split(',')
  
